# 牛剑面试辅导平台 - 启动说明

## 项目状态

✅ **已完成**
- 后端 API（FastAPI + SQLite）
- 前端 React + TypeScript
- 登录认证（JWT）
- 思维训练白板（Canvas）
- 模拟面试室（语音+对话）
- 训练报告
- 历史记录
- 账号管理
- **POE API 已接入**（Claude Sonnet 4.6）

## 本地启动步骤

### 1. 准备环境

**要求：**
- Python 3.9+
- Node.js 18+

### 2. 配置后端

```bash
cd backend

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 配置 API Key
cp .env.example .env
# 编辑 .env，填入你的 POE_API_KEY
# 获取 Key: https://poe.com/api_key

# 启动后端
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### 3. 启动前端（新终端）

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 4. 访问网站

打开浏览器，访问：**http://localhost:5173**

### 5. 登录

- 用户名：`PennyPan`
- 密码：`sisu2026`

## POE API Key 配置

1. 访问 [poe.com/api_key](https://poe.com/api_key)
2. 登录 POE（需要付费订阅）
3. 点击 **Create API Key**
4. 复制 Key（格式：`pk-xxxxx`）
5. 编辑 `backend/.env`，填入：
   ```
   POE_API_KEY=pk-你的密钥
   POE_MODEL=claude-sonnet-4.6
   ```

## 功能说明

| 页面 | 功能 |
|------|------|
| 登录 | JWT 认证，管理员/学生两种角色 |
| 学科选择 | 选择数学或物理，训练或面试模式 |
| 思维训练 | Canvas 白板书写，AI 实时反馈 |
| 模拟面试 | 语音对话，AI 面试官，30分钟计时 |
| 训练报告 | AI 评估报告（评分、思路、建议） |
| 历史记录 | 所有训练记录，筛选排序 |
| 账号管理 | 管理员可增删学生账号 |

## 常见问题

**Q: POE API 报错 500？**
A: 检查 `.env` 中的 `POE_API_KEY` 是否正确，模型名称是否为 `claude-sonnet-4.6`

**Q: 前端无法连接后端？**
A: 检查后端是否启动（http://localhost:8000/docs），CORS 已配置允许所有来源

**Q: 登录失败？**
A: 检查数据库是否已初始化（`oxbridge.db` 文件是否存在），可运行 `python seed.py` 重新初始化
