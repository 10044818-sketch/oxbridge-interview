services:
  - type: web
    name: oxbridge-interview
    env: python
    plan: free
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: POE_API_KEY
        value: sk-poe--tSp8vvoUtXd9SfFaY5MqXkNQcD9lZ4kSdPSncUpwHg
      - key: DATABASE_URL
        value: oxbridge.db
